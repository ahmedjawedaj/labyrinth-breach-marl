# Experiments

This folder is reserved for experiment organization notes, matrix planning, or future manifests.

Current canonical experiment definitions live in:

- `configs/experiment_manifests/`

Current run outputs live in:

- `results/`

Do not place ad hoc generated artifacts here without a documented purpose.
